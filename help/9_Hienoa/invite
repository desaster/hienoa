Synopsis:
   invite <nick> [#channel]

Description:
   This command is a wrapper to epic's INVITE command.  The difference
   is that if no channel is given, current is used.

Aliases:
   i

See Also:
   invite(1);

