Synopsis:
   save

Description:
   This command saves the current configuration, userlist and notifylist.
   This is normally called automatically after making any changes, but hienoa
   can also be configured to not save configuration/userlist/notifylist
   automatically.

See Also:
   config(9)

