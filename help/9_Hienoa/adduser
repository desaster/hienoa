Synopsis:
   adduser <user> [+flag(s) | #channel(s) [+flag(s)] | hostmask(s)]

Description:
   This command adds new user to the userlist.  Initial flags, channels and
   hostmasks can be given as arguments.

   Note that the username doesn't have to be same as the user's IRC nickname.

Examples:
   Add new user foobar
      /adduser foobar

   Add new user blah with flags +ao and hostmask blah!*@*.ibm.com
      /adduser blah +ao blah!*@*.ibm.com
   
   To add new user boob with global flags +a and channel #epic
   with channel-specific flags +po
      /adduser boob +a #epic +po

Aliases:
   useradd

See Also:
   deluser(9); userlist(9)

