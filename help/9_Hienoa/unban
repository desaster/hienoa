Synopsis:
   unban [#channel] <nick|nick!user@host>

Description:
    This commands removes ban from a channel.  If no channel is specified,
    current will be used.

    If a nick is given as argument, the matching hostmask will be determined
    automatically.  If the command is called with "*" argument, all bans
    from the current channel will be removed.

See Also:
   ban(9); mode(1)

