Synopsis:
   mode [#channel|your nickname] <mode parameter(s)>

Description:
   This command is a wrapper to epic's MODE command.  The only difference
   is that if no channel is specified, current one will be used.

See Also:
   mode(1)

