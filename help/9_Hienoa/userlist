Synopsis:
   userlist [search pattern]

Description:
   This command displays the contents of the userlist.  If given an argument,
   only users that match the search pattern are displayed.

   Examples:
       /userlist desa* (lists all users whose name begins with "desa")
       /userlist desaster (lists user named "desaster")

Aliases:
   ul; listusers

See Also:
   adduser(9); userlist(9)

