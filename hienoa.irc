# Copyright (c) 1999-2002 Upi Tamminen <desaster@dragonlight.fi>
# See the COPYRIGHT file for more information

^dump all

unless (word(0 $info(v)) != [epic4] || V < 20031201) {
    @ h.version = [0.53]
    @ h.path = realpath($before(-1 / $word(1 $loadinfo())))
    eval load $h.path/core/core.irc
} else {
    echo !!! ircII-EPIC4 1.2.0 or later is required for this script
    echo !!! Visit http://www.epicsol.org and ftp://ftp.epicsol.org/pub/epic
}

# vim: set ai tw=75 sw=4:
